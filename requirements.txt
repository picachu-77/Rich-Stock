# 이 프로젝트가 필요로 하는 부품(라이브러리) 목록입니다.
# 설치 명령:  pip install -r requirements.txt

pykrx>=1.0.45           # 한국거래소 시세를 가져오는 도구
psycopg2-binary>=2.9.9  # 파이썬 <-> Postgres(Neon) 연결 담당
python-dotenv>=1.0.1    # .env 파일에서 비밀값을 읽어오는 도구
pandas>=2.2.0           # 표 형태 데이터를 다루는 도구
streamlit>=1.40.0       # 웹 화면을 만드는 도구
plotly>=5.22.0          # 차트를 그리는 도구
